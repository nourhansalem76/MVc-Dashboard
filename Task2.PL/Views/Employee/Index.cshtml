@model IEnumerable<EmployeeViewModel>
@{
    ViewData["Title"] = "All Employees";
}
@*<div class="alert alert-success">
    @ViewData["Message"]
</div>*@
@*<div class="alert alert-success">
    @ViewBag.message
</div>*@
@*@if (TempData["Message"] is not null)
{
    <div class="alert alert-success">
        @TempData["Message"]
    </div>
}*@

<div class="row justify-content-center align-items-center my-4">
    <form asp-action="Index" class="row col-8 offset-1 mt-5"> 
        <div class="col-8"> 
            <input type="search" class="form-control" name="SearchValue" placeholder="Search By Name" />
        </div>
        <div class="col-4">
            <input type="submit" class="btn btn-success" value="Search" />
        </div>
    </form>
</div>

<h1>All Employees</h1> <br></br> <br></br>
<a class="btn btn-primary mb-4" asp-controller="Employee" asp-action="Create">Create New Employee</a>
@if(Model.Count()>0)
{
    <table class="table table-striped table-hover">
        <thead>
           <tr> 
                <td>@Html.DisplayNameFor(E => E.Name)</td>
                <td>@Html.DisplayNameFor(E => E.Age)</td>
                <td>@Html.DisplayNameFor(E => E.Address)</td>
                <td>@Html.DisplayNameFor(E => E.Image)</td>
                <td>@Html.DisplayNameFor(E => E.Department)</td>
                <td>@Html.DisplayNameFor(E => E.Salary)</td>
                <td>@Html.DisplayNameFor(E => E.IsActive)</td>
                <td>@Html.DisplayNameFor(E => E.EmaiL)</td>
                <td>@Html.DisplayNameFor(E => E.PhoneNumber)</td>
                <td>@Html.DisplayNameFor(E => E.HireDate)</td>
                <td>Details</td>
                <td>Update</td>
                <td>Delete</td>

           </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <td>@employee.Name</td>
                    <td>@employee.Age</td>
                    <td>@employee.Address</td>
                    <td>
                        <img  src="~/files/img/@employee.ImageName" class="img-fluid rounded" alt="Employee Image" width="30" height="30" />
                    </td>
                    <td>@Html.DisplayFor(M => employee.Department.Name)</td>
                    <td>@Html.DisplayFor(M => employee.Salary)</td>
                    <td>@Html.DisplayFor(M => employee.IsActive)</td>
                    <td>@Html.DisplayFor(M => employee.EmaiL)</td>
                    <td>@employee.PhoneNumber</td>
                    <td>@employee.HireDate</td>
                    <partial name="_ButtonsPartialView" model="@employee.Id.ToString()"/>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div>   
        <h3 class="alert alert-warning">There Is No Employees!!</h3>
    </div>
}




